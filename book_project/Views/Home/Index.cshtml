@{
    ViewData["Title"] = "Home Page / Recommendations";
}

<div class="text-center">
    <h1 class="display-4">Recommended for you</h1>

</div>

@model List<book_project.Models.Book>

@if (!Model.Any())
{
    <p>No recommendations</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Genre</th> 
                <th>Reviews</th> </tr>
        </thead>
        <tbody>
            @foreach (var book in Model)
            {
                <tr>
                    <td>@book.Title</td>
                    <td>@book.Genre</td>
                    <td><a href="http://localhost:5210/reviews/@book.BookId">Reviews</a></td>
                </tr>
            }
        </tbody>
    </table>
}